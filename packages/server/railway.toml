[build]
builder = "nixpacks"
buildCommand = "npm install --include=dev && npm run build -w packages/shared && npm run build -w packages/engine"

[deploy]
startCommand = "npx tsx packages/server/src/index.ts"