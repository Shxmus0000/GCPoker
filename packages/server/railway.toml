[build]
builder = "nixpacks"
buildCommand = "npm run build -w packages/shared && npm run build -w packages/engine && npm run build -w packages/server"

[deploy]
startCommand = "node packages/server/dist/index.js"
